/**
 * Shared enums used across the application
 */

export enum QuickAction {
  GLUCOSE = "glucose",
  INSULIN = "insulin",
  EXERCISE = "exercise",
  FOOD = "food",
}

export enum ActiveSheet {
  NONE = "none",
  GLUCOSE = "glucose",
  INSULIN = "insulin",
  EXERCISE = "exercise",
  FOOD = "food",
}
